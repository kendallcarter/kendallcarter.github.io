/**
 * Write a class named "ArrayClass". In this class must have the following - 
 * 1. a private 2d array of size 3X3
 * 2. public constructor. The constructor will initialize the array with random values
 * 3. public ArrayClass copy constructor - uses the attribute values of the existing object to create a new object 
 * 4. a public method to that will display the array.   
 * 
 * 5. in the main, create a ArrayClass object and call display method
 * create another object using the copy constructor and call display method
 */

class ArrayClass{
    private:
        int a[3][3];

    ArrayClass(int a[][], );


}

ArrayClass( ArrayClass& object); 

int main(){
    ArrayClass 
    return 0;

}